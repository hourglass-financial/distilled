/**
 * @generated by @hourglass-financial/api-factory — DO NOT EDIT.
 *
 * This file is machine-owned; hand edits are overwritten on regeneration.
 * Change the source of truth instead:
 *   - auth scheme / base URL / env var names → the generator's vendor profile
 *   - config primitives → effect's `Config` / `ConfigProvider`
 */
import { ConfigError } from "@hourglass-financial/api-factory-core";
import * as Config from "effect/Config";
import * as Context from "effect/Context";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import type * as Redacted from "effect/Redacted";

/** Quarry production API base URL. Override with `QUARRY_API_URL`. */
export const DEFAULT_BASE_URL = "https://api.quarry.example";

/** Resolved Quarry credentials. The API key stays `Redacted` end to end. */
export interface QuarryConfig {
  readonly apiKey: Redacted.Redacted<string>;
  readonly baseUrl: string;
}

/** Context service carrying the resolved Quarry credentials. */
export class Credentials extends Context.Service<Credentials, QuarryConfig>()(
  "@hourglass-financial/api-factory-quarry/Credentials",
) {}

/** Reads `QUARRY_API_KEY` (redacted) and optional `QUARRY_API_URL` from env. */
export const config: Config.Config<QuarryConfig> = Config.all({
  apiKey: Config.redacted("QUARRY_API_KEY"),
  baseUrl: Config.string("QUARRY_API_URL").pipe(
    Config.withDefault(DEFAULT_BASE_URL),
  ),
});

/**
 * Credentials from the environment. Fails with a typed {@link ConfigError} when
 * `QUARRY_API_KEY` is absent — the caller learns exactly what is missing rather
 * than getting an opaque defect.
 */
export const credentialsFromEnv: Layer.Layer<Credentials, ConfigError> =
  Layer.effect(
    Credentials,
    config.pipe(
      Effect.mapError(
        () =>
          new ConfigError({
            message: "Quarry credentials are not configured.",
          }),
      ),
      Effect.map(Credentials.of),
    ),
  );

/** Credentials from explicit values — useful for tests and multi-tenant hosts. */
export const credentialsOf = (values: QuarryConfig): Layer.Layer<Credentials> =>
  Layer.succeed(Credentials, Credentials.of(values));
