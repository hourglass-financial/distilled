/**
 * @generated by @hourglass-financial/api-factory — DO NOT EDIT.
 *
 * This file is machine-owned; hand edits are overwritten on regeneration.
 * Change the source of truth instead:
 *   - wire shapes → the Orbit OpenAPI spec + the generator's schema mapping
 *   - redaction / decoding behavior → @hourglass-financial/api-factory-core
 *
 * Wire field names are kept verbatim (snake_case) for 1:1 fidelity with the
 * Orbit API; secret fields are typed through core's `Secret` so tokens decode
 * to `Redacted<string>` and never print.
 */
import * as Schema from "effect/Schema";

// ---------------------------------------------------------------------------
// Satellites
// ---------------------------------------------------------------------------

/** Cursor metadata. */
export const PageMeta = Schema.Struct({ after: Schema.NullOr(Schema.String) });
export interface PageMeta extends Schema.Schema.Type<typeof PageMeta> {}

/** An Orbit satellite. */
export const Satellite = Schema.Struct({
  id: Schema.String,
  name: Schema.String,
});
export interface Satellite extends Schema.Schema.Type<typeof Satellite> {}

/** A page of satellites. */
export const SatellitePage = Schema.Struct({
  data: Schema.Array(Satellite),
  meta: PageMeta,
});
export interface SatellitePage extends Schema.Schema.Type<
  typeof SatellitePage
> {}
