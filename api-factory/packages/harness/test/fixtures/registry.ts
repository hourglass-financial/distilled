/** Fixture standing in for a generated client's `src/registry.ts`. */
export const operations = [
  "organizations.create",
  "organizations.delete",
  "organizations.get",
  "organizations.list",
] as const;
