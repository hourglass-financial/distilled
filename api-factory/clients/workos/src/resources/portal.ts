/**
 * @generated by @hourglass-financial/api-factory — DO NOT EDIT.
 *
 * This file is machine-owned; hand edits are overwritten on regeneration.
 * Change the source of truth instead:
 *   - operation surface / wire shapes / errors → the WorkOS OpenAPI spec
 *   - request execution, retry → @hourglass-financial/api-factory-core
 *
 * Uniform per-operation blocks: input schema + type, a declarative descriptor,
 * and a thin exported function that dispatches through `run`.
 */
import {
  BadRequest,
  Forbidden,
  NotFound,
  type Operation,
  UnprocessableEntity,
} from "@hourglass-financial/api-factory-core";
import type * as Effect from "effect/Effect";
import * as Schema from "effect/Schema";
import { run, type WorkosClient, type WorkosError } from "../client.ts";
import { PortalLinkResponse } from "../schemas.ts";

// ===========================================================================
// portal.generateLink — POST /portal/generate_link
// ===========================================================================

export const GenerateLinkInput = Schema.Struct({
  return_url: Schema.optional(Schema.String),
  success_url: Schema.optional(Schema.String),
  organization: Schema.String,
  intent: Schema.optional(
    Schema.Literals([
      "sso",
      "dsync",
      "audit_logs",
      "log_streams",
      "domain_verification",
      "certificate_renewal",
      "bring_your_own_key",
    ]),
  ),
  it_contact_emails: Schema.optional(Schema.Array(Schema.String)),
});
export interface GenerateLinkInput extends Schema.Schema.Type<
  typeof GenerateLinkInput
> {}

const generateLinkErrors = [
  BadRequest,
  Forbidden,
  NotFound,
  UnprocessableEntity,
] as const;

const generateLinkOp: Operation<
  typeof GenerateLinkInput,
  typeof PortalLinkResponse,
  typeof generateLinkErrors
> = {
  id: "portal.generateLink",
  method: "POST",
  retry: "throttling",
  pathTemplate: "/portal/generate_link",
  pathParams: [],
  queryParams: [],
  input: GenerateLinkInput,
  output: PortalLinkResponse,
  errors: generateLinkErrors,
};

/** Generate a Portal Link scoped to an Organization. */
export const generateLink = (
  input: GenerateLinkInput,
): Effect.Effect<
  PortalLinkResponse,
  WorkosError<typeof generateLinkErrors>,
  WorkosClient
> => run(generateLinkOp, input);
