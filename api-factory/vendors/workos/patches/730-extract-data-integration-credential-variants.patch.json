{
  "kind": "raw",
  "ops": [
    {
      "op": "add",
      "path": "/components/schemas/DataIntegrationOAuthCredentialResponse",
      "value": {
        "type": "object",
        "properties": {
          "active": { "type": "boolean", "description": "Indicates credentials are available.", "const": true },
          "credential": {
            "type": "object",
            "properties": {
              "object": { "type": "string", "description": "Distinguishes the credential object.", "const": "credential" },
              "auth_method": { "type": "string", "description": "The authentication method for this credential. Additional values may be added in the future; handle unknown values gracefully.", "x-extensible-enum": ["oauth", "api_key"], "const": "oauth" },
              "value": { "type": "string", "description": "The OAuth access token.", "example": "gho_16C7e42F292c6912E7710c838347Ae178B4a" },
              "expires_at": { "type": ["string", "null"], "description": "The ISO-8601 formatted timestamp indicating when the credential expires.", "example": "2025-12-31T23:59:59.000Z" },
              "scopes": { "type": "array", "items": { "type": "string" }, "description": "The scopes granted to the access token.", "example": ["repo", "user:email"] },
              "missing_scopes": { "type": "array", "items": { "type": "string" }, "description": "If the integration has requested scopes that aren't present on the access token, they're listed here.", "example": [] }
            },
            "required": ["object", "auth_method", "value", "expires_at", "scopes", "missing_scopes"],
            "description": "The credential object containing the vended secret."
          }
        },
        "required": ["active", "credential"]
      }
    },
    {
      "op": "add",
      "path": "/components/schemas/DataIntegrationApiKeyCredentialResponse",
      "value": {
        "type": "object",
        "properties": {
          "active": { "type": "boolean", "description": "Indicates credentials are available.", "const": true },
          "credential": {
            "type": "object",
            "properties": {
              "object": { "type": "string", "description": "Distinguishes the credential object.", "const": "credential" },
              "auth_method": { "type": "string", "description": "The authentication method for this credential. Additional values may be added in the future; handle unknown values gracefully.", "x-extensible-enum": ["oauth", "api_key"], "const": "api_key" },
              "value": { "type": "string", "description": "The API key secret.", "example": "sk-1234567890abcdef" }
            },
            "required": ["object", "auth_method", "value"],
            "description": "The credential object containing the vended secret."
          }
        },
        "required": ["active", "credential"]
      }
    },
    {
      "op": "add",
      "path": "/components/schemas/DataIntegrationCredentialUnavailableResponse",
      "value": {
        "type": "object",
        "properties": {
          "active": { "type": "boolean", "description": "Indicates credentials are not available.", "const": false },
          "error": { "type": "string", "enum": ["not_installed", "needs_reauthorization"], "description": "The reason credentials are unavailable. Additional values may be added in the future; handle unknown values gracefully.\n- `\"not_installed\"`: The user does not have the integration installed.\n- `\"needs_reauthorization\"`: The user needs to reauthorize the integration.", "example": "not_installed", "x-extensible-enum": ["not_installed", "needs_reauthorization"] }
        },
        "required": ["active", "error"]
      }
    },
    { "op": "replace", "path": "/components/schemas/DataIntegrationCredentialsResponse/oneOf/0", "value": { "$ref": "#/components/schemas/DataIntegrationOAuthCredentialResponse" } },
    { "op": "replace", "path": "/components/schemas/DataIntegrationCredentialsResponse/oneOf/1", "value": { "$ref": "#/components/schemas/DataIntegrationApiKeyCredentialResponse" } },
    { "op": "replace", "path": "/components/schemas/DataIntegrationCredentialsResponse/oneOf/2", "value": { "$ref": "#/components/schemas/DataIntegrationCredentialUnavailableResponse" } },
    {
      "op": "replace",
      "path": "/paths/~1data-integrations~1{slug}~1credentials/post/responses/200/content/application~1json/schema",
      "value": {
        "oneOf": [
          { "$ref": "#/components/schemas/DataIntegrationOAuthCredentialResponse" },
          { "$ref": "#/components/schemas/DataIntegrationApiKeyCredentialResponse" },
          { "$ref": "#/components/schemas/DataIntegrationCredentialUnavailableResponse" }
        ]
      }
    }
  ],
  "rationale": "The credentials success union is preserved by extracting its OAuth, API-key, and unavailable members as named components and referencing those components from an inline operation-level oneOf.",
  "precondition": {
    "pointer": "/components/schemas/DataIntegrationCredentialsResponse",
    "test": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "active": { "type": "boolean", "description": "Indicates credentials are available.", "const": true },
            "credential": {
              "type": "object",
              "properties": {
                "object": { "type": "string", "description": "Distinguishes the credential object.", "const": "credential" },
                "auth_method": { "type": "string", "description": "The authentication method for this credential. Additional values may be added in the future; handle unknown values gracefully.", "x-extensible-enum": ["oauth", "api_key"], "const": "oauth" },
                "value": { "type": "string", "description": "The OAuth access token.", "example": "gho_16C7e42F292c6912E7710c838347Ae178B4a" },
                "expires_at": { "type": ["string", "null"], "description": "The ISO-8601 formatted timestamp indicating when the credential expires.", "example": "2025-12-31T23:59:59.000Z" },
                "scopes": { "type": "array", "items": { "type": "string" }, "description": "The scopes granted to the access token.", "example": ["repo", "user:email"] },
                "missing_scopes": { "type": "array", "items": { "type": "string" }, "description": "If the integration has requested scopes that aren't present on the access token, they're listed here.", "example": [] }
              },
              "required": ["object", "auth_method", "value", "expires_at", "scopes", "missing_scopes"],
              "description": "The credential object containing the vended secret."
            }
          },
          "required": ["active", "credential"]
        },
        {
          "type": "object",
          "properties": {
            "active": { "type": "boolean", "description": "Indicates credentials are available.", "const": true },
            "credential": {
              "type": "object",
              "properties": {
                "object": { "type": "string", "description": "Distinguishes the credential object.", "const": "credential" },
                "auth_method": { "type": "string", "description": "The authentication method for this credential. Additional values may be added in the future; handle unknown values gracefully.", "x-extensible-enum": ["oauth", "api_key"], "const": "api_key" },
                "value": { "type": "string", "description": "The API key secret.", "example": "sk-1234567890abcdef" }
              },
              "required": ["object", "auth_method", "value"],
              "description": "The credential object containing the vended secret."
            }
          },
          "required": ["active", "credential"]
        },
        {
          "type": "object",
          "properties": {
            "active": { "type": "boolean", "description": "Indicates credentials are not available.", "const": false },
            "error": { "type": "string", "enum": ["not_installed", "needs_reauthorization"], "description": "The reason credentials are unavailable. Additional values may be added in the future; handle unknown values gracefully.\n- `\"not_installed\"`: The user does not have the integration installed.\n- `\"needs_reauthorization\"`: The user needs to reauthorize the integration.", "example": "not_installed", "x-extensible-enum": ["not_installed", "needs_reauthorization"] }
          },
          "required": ["active", "error"]
        }
      ]
    }
  },
  "blastRadius": {
    "role": "response",
    "clears": [
      { "rule": "openapi.response.success", "construct": "/paths/~1data-integrations~1{slug}~1credentials/post/responses/200/content/application~1json/schema" }
    ]
  },
  "provenance": {
    "evidenceType": "vendor-docs-citation",
    "url": "https://workos.com/docs/reference/pipes/vend-credentials",
    "fetchedAt": "2026-07-21",
    "observed": "Vend credentials returns one of an OAuth credential, an API-key credential, or an inactive response explaining why credentials are unavailable.",
    "specd": "The operation success remains an inline union of named references so every documented response variant decodes without collapsing the contract.",
    "authoredAgainstSpecHash": "sha256-e2d7b0f947f5e7c4c212dbd460957264df4d7a3018d30bde5c03d79997614664",
    "reporter": "ticket-50-onboarding"
  },
  "id": "730-extract-data-integration-credential-variants"
}
