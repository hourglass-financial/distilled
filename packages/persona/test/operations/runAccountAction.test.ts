import { describe, it } from "vitest";

// Coverage: fixture-dependent
describe("runAccountAction", () => {
  it.todo(
    "live success requires a configured account action identifier that is safe to run on a disposable account",
  );
});
