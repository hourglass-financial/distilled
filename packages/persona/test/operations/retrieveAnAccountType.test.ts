import { describe, it } from "vitest";

// Coverage: fixture-dependent
describe("retrieveAnAccountType", () => {
  it.todo(
    "live success requires a stable account type identifier configured in the sandbox",
  );
});
