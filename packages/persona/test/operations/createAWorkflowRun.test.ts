import { describe, it } from "vitest";

// Coverage: fixture-dependent
describe("createAWorkflowRun", () => {
  it.todo(
    "live success requires a configured workflow with safe test inputs and a disposable owned run",
  );
});
