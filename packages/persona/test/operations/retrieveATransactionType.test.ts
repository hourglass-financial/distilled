import { describe, it } from "vitest";

// Coverage: fixture-dependent
describe("retrieveATransactionType", () => {
  it.todo(
    "live success requires a stable transaction type identifier configured in the sandbox",
  );
});
