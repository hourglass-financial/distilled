import { describe, it } from "vitest";

// Coverage: environment-dependent
describe("retrieveAUserAuditLog", () => {
  it.todo(
    "the shared credential cannot list user audit logs, so no safe audit-log identifier is available to retrieve",
  );
});
