import { describe, it } from "vitest";

// Coverage: environment-dependent
describe("listAllUserAuditLogs", () => {
  it.todo(
    "live success requires user-audit-log permission; the shared sandbox credential returned Unauthorized on 2026-07-16",
  );
});
