import { describe, it } from "vitest";

// Coverage: fixture-dependent
describe("transactionsAddTag", () => {
  it.todo(
    "live success requires a writable transaction type and a disposable owned transaction with the required relationships",
  );
});
