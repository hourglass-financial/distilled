import { describe, it } from "vitest";

// Coverage: fixture-dependent
describe("createAGraphQuery", () => {
  it.todo(
    "live success requires a configured graph query and stable graph data in the sandbox",
  );
});
