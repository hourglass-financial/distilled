import { describe, it } from "vitest";

// Coverage: environment-dependent
describe("listAllInquiryTemplates", () => {
  it.todo(
    "live success requires inquiry-template metadata permission; the shared sandbox credential returned Unauthorized on 2026-07-16",
  );
});
